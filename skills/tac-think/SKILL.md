---
name: tac-think
description: Alias for /tac-ask — adaptive Q&A that reads the codebase first, asks smart questions, and captures decisions
argument-hint: "<feature-description>"
---

# TAC THINK — Alias for TAC ASK

This is an alias for `/tac-ask`. Invoke `/tac-ask` with the same arguments.

When this skill is triggered, immediately invoke the `tac-ask` skill with whatever arguments were provided. Do not duplicate the ASK workflow here — all logic lives in `/tac-ask`.
